#!/usr/bin/env bash
<% if @environment == 'production' %>

# Send weekly reminders to coordinators.
sudo su <%= @unixname %> bash -c "\
<%= @root_dir %>/bin/./virtualenv_send_coordinator_reminders.sh"
<% end %>
